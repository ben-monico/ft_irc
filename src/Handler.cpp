#include <Handler.hpp>

void Handler::send_all(int client_id, std::string const &to_send)
{
	//send whole message checking bytes sent
}

void Handler::recv_all(int client_id)
{
	//recv whole message checking bytes received
}
